#include <stdio.h>
#include <math.h>
// #include "function.h"
int multiplication(int num1,int num2)
{
    
    if(num1==0 ||num2==0)
    {
        printf("Any number multiplies with '0' is equals to 0!!!\n");
    }
    else
    {
       return num1*num2;
        
    }
    
}